version: "3.9"

services:
  postgres:
    image: postgres:15.8
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sns-db
    ports:
      - 5588:5432
